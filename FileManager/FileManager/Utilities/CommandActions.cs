namespace FileManager.Utilities
{
    enum CommandActions
    {
        //Directories
        pwd=0,
        dir=1,
        mkdir=2,
        rndir=3,
        copydir = 4,
        movedir =5,
        rmdir=6,

        //Files
        mkfile = 7,
        rnfile = 8,
        copyfile = 9,
        movefile = 10,
        rmfile = 11,
    }
}
